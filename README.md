# ООО «СтройМатериалы» - демонстрационный экзамен

Проект выполнен для задания демонстрационного экзамена 09.02.07.

## Технологии

- C# / WPF / .NET 8
- SQLite как переносимая локальная БД файлом
- `Microsoft.Data.Sqlite`

## Быстрый запуск

1. Клонировать репозиторий.
2. Запустить `run.bat`.

`run.bat` сначала пробует открыть готовую публикацию из `publish/win-x64`. Для готового exe нужен .NET 8 Windows Desktop Runtime. Если публикации нет, скрипт запускает проект командой:

```powershell
dotnet run --project StroyMaterials.App/StroyMaterials.App.csproj
```

Для запуска из исходников нужен .NET 8 SDK или новее.

## Учетные записи для проверки

| Роль | Логин | Пароль |
|---|---|---|
| Администратор | `94d5ous@gmail.com` | `uzWC67` |
| Менеджер | `1diph5e@tutanota.com` | `8ntwUp` |
| Авторизованный клиент | `5d4zbu@tutanota.com` | `rwVDh9` |

Также доступен вход без учетной записи в роли гостя.

## База данных

Основная БД находится в файле:

```text
StroyMaterials.App/Data/stroymaterials.db
```

При сборке файл копируется рядом с приложением. Если БД в папке запуска отсутствует, приложение создает ее из SQL-скриптов:

```text
StroyMaterials.App/Database/create_schema.sql
StroyMaterials.App/Database/seed_data.sql
```

Для возврата к исходному состоянию можно запустить `reset_database.bat`.

## Реализовано

- авторизация и вход гостем;
- роли: гость, авторизованный клиент, менеджер, администратор;
- WPF-интерфейс с единым стилем по руководству задания;
- список товаров из БД с изображениями и заглушкой `picture.png`;
- подсветка скидки больше 12%;
- голубая подсветка товаров без остатка;
- старая цена красная и зачеркнутая, итоговая цена рядом;
- поиск, фильтрация и сортировка для менеджера и администратора;
- добавление, редактирование и удаление товаров администратором;
- запрет удаления товара, который присутствует в заказе;
- просмотр заказов менеджером и администратором;
- добавление, редактирование и удаление заказов администратором;
- резервная копия БД в формате `.bak`;
- отчеты и схемы в папке `docs`.

## Документы и схемы

Папка `docs` содержит:

- `ER_диаграмма.pdf`;
- `Блок_схема_алгоритма.pdf`;
- `Диаграмма_прецедентов.drawio`;
- `Диаграмма_прецедентов.pdf`;
- `Отчет_о_проверке_работы_системы.docx`;
- `Отчет_о_проверке_работы_системы.pdf`;
- `Отчет_резервное_копирование_и_мониторинг_БД.docx`;
- `Отчет_резервное_копирование_и_мониторинг_БД.pdf`;
- `Тестовые_сценарии.docx`;
- `Тестовые_сценарии.pdf`;
- `backups/UserXX_РКБД_Дмитриенко.bak`.

Документы оформлены с учетом ГОСТ-профиля из `ai-vkr-gost-kit`: Times New Roman, поля 30/15/20/20 мм, основной текст 14 pt, таблицы 12 pt.
